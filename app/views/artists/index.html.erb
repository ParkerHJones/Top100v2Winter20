<h1>Artists</h1>
<%= link_to 'Add Artist', new_artist_path %>
<h2><%= @artists.length <= 0 ? "No artists" : '' %></h2>
<ul>
 <% @artists.each do |artist| %>
    <li>
        <%= link_to artist.name, artist %>
    </li>
<% end %>
</ul>

