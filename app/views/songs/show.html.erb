<h1><%= @song.title %></h1>

<h2>Songs</h2>
<ul>
 <% @song.artists.each do |artist| %>
    <li>
        <%= link_to artist.name, artist %>
    </li>

    <% end %>
</ul>

<%= link_to 'Delete Song', @song, method: :delete %>